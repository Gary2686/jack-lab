<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <!--
    本站為 hash-routed SPA：所有分頁（#/about、#/team、#/research、…）
    其實都由 index.html + JS 動態渲染，Google 也是把它們視為同一個 URL。
    依 Google sitemap 規範，URL 中不應含 # fragment，因此只列出首頁。
    Hash-routed SPA: every section is served by index.html, so we list a single
    canonical URL here (Google treats fragments as part of the same page).
  -->
  <url>
    <loc>https://gary2686.github.io/jack-lab/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
